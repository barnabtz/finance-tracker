<nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container">
        <%= link_to 'Finance Tracker', root_path, class: 'navbar-brand' %>
        <% if current_user %>
          <button class="navbar-toggler d-lg-none" type="button" data-toggle="collapse" data-target="#collapsibleNavId" aria-controls="collapsibleNavId"
              aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="collapsibleNavId">
            <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
              <li class="nav-item <%= 'active' if request.path == my_portfolio_path %> ">
                <%= link_to 'My Portfolio', my_portfolio_path, class: 'nav-link' %>
              </li>
              <li class="nav-item <%= 'active' if request.path == my_friends_path %> ">
                <%= link_to 'My Friends', my_friends_path, class: 'nav-link' %>
              </li>
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="dropdownId" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Profile</a>
                <div class="dropdown-menu" aria-labelledby="dropdownId">
                  <%= link_to edit_user_registration_path, class: 'dropdown-item' do %>
                    <%= fa_icon 'edit' %>  Edit Profile
                  <% end %>
                  <a class="dropdown-item" href="#">Action 2</a>
                </div>
              </li>
            </ul>
            <ul class="navbar-nav ml-auto">
              <li class="nav-item user-name">
                <%= fa_icon 'user', class: 'user-name' %><%= current_user.full_name %>
              </li>
              <li class="nav-item">
                <%= link_to("Logout", destroy_user_session_path, class: "btn btn-xs btn-danger px-1", method: :delete) %>
              </li>
            </ul>
          <% end %>
        </div>
      </div>
    </nav>